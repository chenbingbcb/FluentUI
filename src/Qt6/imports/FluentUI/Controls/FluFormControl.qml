import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import FluentUI

//表单控件基类模板
Item {
    // property var config: ({}) //配置 来自动态FluLoader的config属性 或者静态实例化时再定义
    // property var value: null //数值 来自动态FluLoader的value属性 或者静态实例化时再定义

    function initDisplay() {
    }
}
